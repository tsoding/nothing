#ifndef MINMAX_H_
#define MINMAX_H_

#define min_int64(a, b) (a < b ? a : b)
#define max_int64(a, b) (a > b ? a : b)
#define min_size(a, b) (a < b ? a : b)
#define max_size(a, b) (a > b ? a : b)

#endif  // MINMAX_H_
