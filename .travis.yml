language: c
addons:
  homebrew:
    packages:
      - sdl2
os:
  - osx
compiler:
  - clang
  - gcc
script:
  - mkdir build/
  - cd build/
  - cmake ..
  - make -k
  - ./nothing_test
  - cd ../levels/
  - ../build/devtools/svg2level/svg2level level-01.svg platforms.svg ../data/levels/
