language: c
addons:
  homebrew:
    packages:
      - sdl2
      - cmake
    update: true
os:
  - osx
compiler:
  - clang
  - gcc
script:
  - brew info sdl2 # for debug test
  - mkdir build/
  - cd build/
  - cmake -DSDL2_INCLUDE_DIR=/usr/local/include ..
  - make -k
  - ./nothing_test
